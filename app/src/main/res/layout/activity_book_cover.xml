<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".BookCover">

    <ImageView
            android:id="@+id/coverPage"
            app:srcCompat="@drawable/blank_cover"
            android:layout_width="400dp"
            android:layout_height="530dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginTop="16dp"/>
    <Button
            android:id="@+id/startBookBtn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@+id/coverPage"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginStart="75dp"
            android:background="@drawable/rounded_button"
            android:text="@string/cover_screen_btn"
            android:typeface="serif"
            android:textStyle="bold"/>
    <Button
            android:id="@+id/checkProgressBtn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@+id/startBookBtn"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginTop="10dp"
            android:layout_marginStart="50dp"
            android:background="@drawable/rounded_button"
            app:backgroundTint="@color/yellow_tan"
            android:text="@string/check_progress_btn"
            android:typeface="serif"
            android:textStyle="bold"/>
</androidx.constraintlayout.widget.ConstraintLayout>